<%= form_for(@cliente_vehiculo) do |f| %>
  <% if @cliente_vehiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente_vehiculo.errors.count, "error") %> prohibited this cliente_vehiculo from being saved:</h2>

      <ul>
      <% @cliente_vehiculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cliente_id %><br>
    <%= f.text_field :cliente_id %>
  </div>
  <div class="field">
    <%= f.label :vehiculo_id %><br>
    <%= f.text_field :vehiculo_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
